namespace SenseNet.Portal.UI
{
    public enum ContentViewMode
    {
        None = 0,
        FramedBrowse,
        Browse,     // inline, no frame
        Edit,
        InlineEdit, // inline, no frame
        New,
        InlineNew   // inline, no frame
    }
}
